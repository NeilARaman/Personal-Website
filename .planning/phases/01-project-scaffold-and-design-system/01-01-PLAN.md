---
phase: 01-project-scaffold-and-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - package.json
  - tsconfig.json
  - src/pages/index.astro
  - .gitignore
autonomous: true
requirements: [TECH-01, TECH-02]

must_haves:
  truths:
    - "Astro project builds successfully with zero client-side JS in output"
    - "Old Next.js files are removed from the redesign branch"
    - "articles/ directory and data/tools.js are preserved intact"
    - "No CSS-in-JS runtime exists in dependencies"
  artifacts:
    - path: "package.json"
      provides: "Astro 6 as sole framework dependency"
      contains: "astro"
    - path: "astro.config.mjs"
      provides: "Astro configuration with site URL"
      contains: "defineConfig"
    - path: "src/pages/index.astro"
      provides: "Minimal index page proving Astro works"
  key_links:
    - from: "package.json"
      to: "astro.config.mjs"
      via: "astro dependency powers the build"
      pattern: "astro"
---

<objective>
Create the redesign branch, remove old Next.js files, and scaffold a fresh Astro 6 project in the repo root.

Purpose: Establish the technical foundation -- a building Astro project with zero legacy code -- so that all subsequent plans build on a clean base.
Output: A deployable Astro project on the `redesign` branch with `npm run dev` and `npm run build` working.
</objective>

<execution_context>
@C:/Users/neilr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/neilr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold-and-design-system/01-CONTEXT.md
@.planning/phases/01-project-scaffold-and-design-system/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create redesign branch and remove old Next.js files</name>
  <files>.gitignore</files>
  <action>
1. Create and switch to a `redesign` branch from `main`: `git checkout -b redesign`
2. Remove all old Next.js files and directories EXCEPT the following which must be preserved:
   - `articles/` directory (markdown content)
   - `data/tools.js` (tool categories data)
   - `.planning/` directory (project planning docs)
   - `CLAUDE.md` (project instructions -- will be updated later)
   - `.git/` (obviously)
3. Files/directories to remove: `pages/`, `layouts/`, `components/`, `lib/`, `public/`, `data/` (all files EXCEPT `tools.js`), `stitches.config.js`, `next.config.js`, `next-sitemap.config.js`, `package.json`, `package-lock.json`, `bun.lock`, `tsconfig.json`, `.eslintrc.json`, `.prettierrc`, `.prettierrc.json`, `vercel.json`, `.nvmrc`, `node_modules/`, `.next/`
4. Commit the removal: "chore(01-01): remove old Next.js files on redesign branch"

IMPORTANT: Do NOT remove `articles/introducing-foundry.md` or `data/tools.js`. These carry over to the new site.
  </action>
  <verify>
    <automated>git status && test -f articles/introducing-foundry.md && test -f data/tools.js && test ! -f next.config.js && test ! -d pages/</automated>
  </verify>
  <done>Redesign branch exists, old Next.js files are gone, articles/ and data/tools.js are preserved</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Astro 6 project and verify build</name>
  <files>astro.config.mjs, package.json, tsconfig.json, src/pages/index.astro, .gitignore</files>
  <action>
1. Initialize a new Astro project manually (do not use `npm create astro` as it would overwrite preserved files):
   - Create `package.json` with:
     - `name`: "neilraman-com"
     - `type`: "module"
     - `scripts`: `{ "dev": "astro dev", "build": "astro build", "preview": "astro preview" }`
   - Run `npm install astro@^6.1` to install Astro 6
2. Create `astro.config.mjs`:
   ```javascript
   import { defineConfig } from 'astro/config';
   export default defineConfig({
     site: 'https://neilraman.com',
   });
   ```
3. Create `tsconfig.json` with `{ "extends": "astro/tsconfigs/strict" }` (Astro requires this even for JS projects)
4. Create `src/pages/index.astro` with a minimal placeholder:
   ```astro
   ---
   // Placeholder -- replaced in Plan 03
   ---
   <html lang="en">
     <head>
       <meta charset="utf-8" />
       <meta name="viewport" content="width=device-width, initial-scale=1" />
       <title>Neil Raman</title>
     </head>
     <body>
       <h1>Neil Raman</h1>
       <p>Site under construction.</p>
     </body>
   </html>
   ```
5. Update `.gitignore` to include Astro-specific entries: `node_modules/`, `dist/`, `.astro/`
6. Run `npm run build` to verify the project compiles successfully
7. Run `npm run dev` briefly to verify dev server starts (then stop it)
8. Commit: "feat(01-01): scaffold Astro 6 project"

Verify NO CSS-in-JS packages exist: `grep -c "styled-components\|@emotion\|@stitches" package.json` should return 0.
  </action>
  <verify>
    <automated>cd "C:/Users/neilr/Documents/Code/Neil-Raman-Personal-Website/neilraman.com" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Astro 6 project builds cleanly, dev server starts, no CSS-in-JS in dependencies, package.json has only astro as framework dep</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- `dist/` output contains HTML but no JS bundles (static site)
- `articles/introducing-foundry.md` exists and is unchanged from main branch
- `data/tools.js` exists and is unchanged from main branch
- `git branch` shows `redesign` as current branch
- `grep -c "stitches\|emotion\|styled-components\|react" package.json` returns 0
</verification>

<success_criteria>
A clean Astro 6 project exists on the `redesign` branch. It builds and serves a placeholder page. No Next.js or React code remains. The articles/ directory and data/tools.js are intact.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-and-design-system/01-01-SUMMARY.md`
</output>
