---
phase: 01-project-scaffold-and-design-system
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/layouts/Base.astro
  - src/components/NavyDot.astro
  - src/components/Nav.astro
  - src/pages/index.astro
autonomous: false
requirements: [FOUN-04, DESG-04]

must_haves:
  truths:
    - "Test page renders with Lora headings and Inter body text visibly distinct"
    - "Page uses semantic HTML: header, nav, main, footer elements present in source"
    - "Navy dot separator is visible as the signature identity element"
    - "Navigation links are aligned to the right side of the header"
    - "Content is constrained to ~720px width with horizontal padding on mobile"
    - "Layout responds correctly on narrow viewports (no horizontal scroll, text does not touch edges)"
  artifacts:
    - path: "src/layouts/Base.astro"
      provides: "Semantic HTML shell wrapping all pages"
      contains: "<header>"
      min_lines: 30
    - path: "src/components/Nav.astro"
      provides: "Right-aligned navigation component"
      contains: "nav-links"
    - path: "src/components/NavyDot.astro"
      provides: "Signature navy dot separator element"
      contains: "navy-dot"
    - path: "src/pages/index.astro"
      provides: "Test page exercising all design tokens and layout"
      contains: "Base"
  key_links:
    - from: "src/layouts/Base.astro"
      to: "src/styles/tokens.css"
      via: "CSS import in layout head"
      pattern: "tokens\\.css"
    - from: "src/layouts/Base.astro"
      to: "src/components/Nav.astro"
      via: "component import and render"
      pattern: "Nav"
    - from: "src/pages/index.astro"
      to: "src/layouts/Base.astro"
      via: "layout wrapper"
      pattern: "Base"
---

<objective>
Build the semantic HTML layout shell, navigation, navy dot signature element, and a test page that exercises the full design system.

Purpose: This is the visual proof that Phase 1 works -- a rendered page showing typography, color, spacing, layout, and the signature identity element all working together. The layout becomes the wrapper for every page in subsequent phases.
Output: Base layout component, Nav component, NavyDot component, and a test page demonstrating the design system.
</objective>

<execution_context>
@C:/Users/neilr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/neilr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold-and-design-system/01-CONTEXT.md
@.planning/phases/01-project-scaffold-and-design-system/01-RESEARCH.md
@.planning/phases/01-project-scaffold-and-design-system/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Base layout, Nav component, and NavyDot component</name>
  <files>src/layouts/Base.astro, src/components/Nav.astro, src/components/NavyDot.astro</files>
  <action>
1. Create `src/components/NavyDot.astro` -- the signature identity element (per user decision: small navy dot as separator/bullet):
   ```astro
   <span class="navy-dot" aria-hidden="true"></span>
   <style>
     .navy-dot {
       display: inline-block;
       width: 6px;
       height: 6px;
       border-radius: 50%;
       background-color: var(--color-accent);
     }
   </style>
   ```

2. Create `src/components/Nav.astro` -- navigation with links right-aligned (per user decision):
   - Flex layout: site name on left, nav links on right
   - Site name "Neil Raman" in heading font (Lora), no underline
   - Links: "Articles", "Tools" in body font, accent color
   - Max-width constrained to `var(--content-width)` with horizontal padding
   - On mobile: stack vertically or keep horizontal with smaller gap
   - Use scoped `<style>` block referencing design tokens via `var()`

3. Create `src/layouts/Base.astro` -- the semantic HTML shell:
   - Accept `title` prop (default: "Neil Raman")
   - `<html lang="en">`
   - `<head>`: charset, viewport, title, Font components for Lora + Inter (from Astro Fonts API), import tokens.css + reset.css + global.css
   - `<body>`:
     - `<header>` containing the Nav component
     - `<main>` with content-width constraint (`max-width: var(--content-width); margin: 0 auto; padding: 0 var(--space-md);`) wrapping `<slot />`
     - `<footer>` with copyright and LinkedIn link
   - CRITICAL: Always pair max-width with horizontal padding to prevent text touching screen edges on mobile (per research pitfall #4)
   - Import CSS files ONCE here -- components access tokens via var(), they do NOT re-import token files

4. Run `npm run build` to verify layout compiles
5. Commit: "feat(01-03): create Base layout, Nav, and NavyDot components"

The Font component import may be `import { Font } from 'astro:assets'` or another path depending on Astro 6 stable API. If the Font component approach fails, the fonts should already work via the astro.config.mjs Fonts API -- the layout just needs to import the CSS files and the fonts will be injected automatically.
  </action>
  <verify>
    <automated>cd "C:/Users/neilr/Documents/Code/Neil-Raman-Personal-Website/neilraman.com" && test -f src/layouts/Base.astro && test -f src/components/Nav.astro && test -f src/components/NavyDot.astro && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Base.astro wraps pages with semantic HTML (header, nav, main, footer). Nav.astro renders right-aligned navigation. NavyDot.astro renders the signature dot. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create test page exercising the full design system</name>
  <files>src/pages/index.astro</files>
  <action>
1. Replace the placeholder `src/pages/index.astro` with a design system test page:
   - Wrap in `Base` layout: `import Base from '../layouts/Base.astro'`
   - Include ALL of these to exercise every design token:
     - `<h1>` through `<h4>` headings (proves type scale + Lora font)
     - A paragraph of body text (proves Inter font + line-height + text color)
     - A link (proves accent color + hover state)
     - A `<NavyDot />` used as a section separator between content blocks (proves DESG-04 signature element)
     - Muted text using `color: var(--color-muted)` (proves muted token)
     - A horizontal rule using `border-color: var(--color-rule)` (proves rule token)
   - The page should feel like a typography specimen -- it is proving the design system works, not delivering final content
   - Add a brief note at the top: "Design System Test" as h1

2. Run `npm run build` to verify the page builds
3. Verify the built HTML at `dist/index.html`:
   - Contains `<header>`, `<nav>`, `<main>`, `<footer>` elements (FOUN-04)
   - Contains no `<script>` tags with JS bundles (TECH-01 -- zero client JS)
   - Contains font references (DESG-02 -- self-hosted fonts)
4. Commit: "feat(01-03): create design system test page with full token exercise"
  </action>
  <verify>
    <automated>cd "C:/Users/neilr/Documents/Code/Neil-Raman-Personal-Website/neilraman.com" && npm run build && grep -c "<header>\|<nav>\|<main>\|<footer>" dist/index.html</automated>
  </verify>
  <done>Test page renders with all design tokens visible: Lora headings, Inter body, off-white background, navy accent, type scale, spacing, and navy dot separator. Semantic HTML elements are present in output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of design system</name>
  <files>src/pages/index.astro</files>
  <action>
  Human verifies the rendered design system test page matches the intended editorial aesthetic.
  What was built: Complete design system with typography, colors, spacing, layout shell, and navy dot signature element -- rendered on a test page.
  </action>
  <verify>
    1. Run `npm run dev` in the project directory
    2. Open http://localhost:4321 in browser
    3. Verify typography:
       - Headings use Lora serif font (warm, literary feel)
       - Body text uses Inter sans-serif (crisp, modern)
       - Heading sizes follow a clear scale (h1 largest, h4 smallest)
       - Body line-height feels comfortable (1.5-1.6)
    4. Verify colors:
       - Background is warm off-white (cream, not stark white)
       - Body text is dark gray, headings are near-black
       - Links are deep navy blue
    5. Verify layout:
       - Content is centered and constrained (~720px wide)
       - Navigation has "Neil Raman" on left, links on right
       - Resize to mobile width -- no horizontal scrollbar, text does not touch edges
    6. Verify signature element:
       - A small navy dot appears as a section separator
    7. Verify semantic HTML:
       - View page source -- confirm header, nav, main, footer elements
    8. Overall feel: Does this look editorial, calm, and considered? (inspired by abhayvenkatesh.com but unique to Neil)
  </verify>
  <done>User approves the visual design -- typography, colors, spacing, layout, and signature element all match the intended editorial aesthetic</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- Built `dist/index.html` contains semantic elements: `<header>`, `<nav>`, `<main>`, `<footer>`
- Built output contains no JS script bundles (Astro static = zero JS)
- Font files are included in build output
- Navy dot element renders visually
- Page displays correctly at both desktop (~1200px) and mobile (~375px) widths
</verification>

<success_criteria>
A test page at localhost:4321 renders the complete design system: Lora headings, Inter body, warm off-white background, deep navy accents, modular type scale, comfortable spacing, right-aligned nav, semantic HTML layout, and the navy dot signature element. The page responds correctly on mobile. User approves the visual design.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-and-design-system/01-03-SUMMARY.md`
</output>
